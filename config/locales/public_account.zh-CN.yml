'zh-CN':
  activerecord:
    models:
      weixin_pam/public_account: 公众号
    attributes:
      weixin_pam/public_account:
        name: 公众号名称
        app_id: AppID(应用ID)
        app_secret: AppSecret(应用密钥)
        api_url: URL(服务器地址)
        api_token: Token(令牌)
        enabled: 启用